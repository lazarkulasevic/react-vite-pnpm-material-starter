import { Box, Typography } from '@mui/material'

const HomePage = () => {
  return (
    <Box>
      <Typography variant="h3">React + Vite + Pnpm + Material</Typography>
      <Typography variant="h4">Starter Template</Typography>
    </Box>
  )
}

export default HomePage
